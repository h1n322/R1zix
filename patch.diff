--- RiskMate.MathEngine/RiskMate.MathEngine/RiskEngine.cs
+++ RiskMate.MathEngine/RiskMate.MathEngine/RiskEngine.cs
@@ -149,9 +149,9 @@
 
                 result.ChartPoints.Add(new ChartPointData
                 {
-                    Name = h.Date.ToString("yyyy-MM-dd"),
+                    Date = h.Date,
                     History = h.Price,
                     Forecast = forecastVal,
                     LowerBound = null,
                     UpperBound = null
                 });
@@ -189,9 +189,9 @@
 
                 result.ChartPoints.Add(new ChartPointData
                 {
-                    Name = futureDate.ToString("yyyy-MM-dd"),
+                    Date = futureDate,
                     History = null,
                     Forecast = Math.Round(representativePath[day], 2),
                     Actual = actualVal,
                     LowerBound = Math.Round(dayPrices[lowerIndex], 2),
@@ -215,9 +215,10 @@
             if (range <= 0)
             {
                 bins.Add(new HistogramBinData
                 {
-                    BinRange = $"${Math.Round(min, 1)}",
+                    MinValue = min,
+                    MaxValue = max,
                     Frequency = finalPrices.Count
                 });
                 return bins;
             }
@@ -237,9 +238,10 @@
                 double binMin = min + (i * binWidth);
                 double binMax = binMin + binWidth;
                 bins.Add(new HistogramBinData
                 {
-                    BinRange = $"${Math.Round(binMin, 1)}-${Math.Round(binMax, 1)}",
+                    MinValue = binMin,
+                    MaxValue = binMax,
                     Frequency = counts[i]
                 });
             }
